Creating an Admin Super User and First Look at Django Admin Interface:
  1) Django admin login graphically interact with models and databases.
  2) Need to create a user account.
  3) python manage.py createsuperuser.
  4) Admin interface is organized by separating by apps and then the models and tables within that app.

Registering the Job Board Model in the Admin:
  1) Everything in Python is an object, and everything is rendered as a string.

Making the Job Postings Appear Better in the Admin Section:
  1) Dunder string method is what gets returned basically as the string format of the object.

Creating, Updating and Deleting Job Postings via the Django Admin:
  1) The admin site provides a nice way to interact with your data and database.

  